name: Hello World
on: 
    push:
        branches:
            - master
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        uses: actions/checkout@v4
      - name: Print Hello World
        run: echo "Hello World"
